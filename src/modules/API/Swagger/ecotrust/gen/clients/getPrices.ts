/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable @typescript-eslint/ban-ts-comment */
// @ts-nocheck
/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/modules/API/Http/mutator.ts'
import fetch from '@/modules/API/Http/mutator.ts'

import type {
  GetPrices500,
  GetPricesQueryResponse,
} from '../models/GetPrices.ts'

function getGetPricesUrl() {
  const res = { method: 'GET', url: `/api/prices` as const }
  return res
}

/**
 * @description Returns a list of all available prices including:
 * - **Currencies** (USD, EUR, AED, etc.)
 * - **Cryptocurrencies** (USDT, BTC, ETH, etc.)
 * - **Gold** (18K gold, etc.)
 * - **Coins** (Emami gold coin, half coin, etc.)
 * All prices are quoted in Iranian Toman (IRT).
 * @summary Get all prices
 * {@link /api/prices}
 */
export async function getPrices(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetPricesQueryResponse,
    ResponseErrorConfig<GetPrices500>,
    unknown
  >({ method: 'GET', url: getGetPricesUrl().url.toString(), ...requestConfig })
  return res.data
}
