/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable @typescript-eslint/ban-ts-comment */
// @ts-nocheck
/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@kubb/plugin-client/clients/axios'
import type {
  QueryClient,
  QueryKey,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

import { getPrices } from '../clients/getPrices.ts'
import type {
  GetPrices500,
  GetPricesQueryResponse,
} from '../models/GetPrices.ts'

export const getPricesSuspenseQueryKey = () => [{ url: '/api/prices' }] as const

export type GetPricesSuspenseQueryKey = ReturnType<
  typeof getPricesSuspenseQueryKey
>

export function getPricesSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPricesSuspenseQueryKey()
  return queryOptions<
    GetPricesQueryResponse,
    ResponseErrorConfig<GetPrices500>,
    GetPricesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getPrices({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * @description Returns a list of all available prices including:
 * - **Currencies** (USD, EUR, AED, etc.)
 * - **Cryptocurrencies** (USDT, BTC, ETH, etc.)
 * - **Gold** (18K gold, etc.)
 * - **Coins** (Emami gold coin, half coin, etc.)
 * All prices are quoted in Iranian Toman (IRT).
 * @summary Get all prices
 * {@link /api/prices}
 */
export function useGetPricesSuspense<
  TData = GetPricesQueryResponse,
  TQueryKey extends QueryKey = GetPricesSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetPricesQueryResponse,
        ResponseErrorConfig<GetPrices500>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? getPricesSuspenseQueryKey()

  const query = useSuspenseQuery(
    {
      ...getPricesSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<GetPrices500>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
